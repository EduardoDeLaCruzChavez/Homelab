server {
  server_name service.homelab.cz;

  include /etc/nginx/conf.d/global.conf;


  set $upstream_container http://container:8080;

  location / {
    include snippets/authelia-protect.conf;
    proxy_pass $upstream_container;
    include snippets/proxyHeader.conf;
  }
}

